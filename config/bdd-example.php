<?php

// You need to replace dbhost with the database host
define('DB_HOST', 'dbhost');

// You need to replace dbname with the database name
define('DB_NAME', 'dbname');

// You need to replace username with the user name of the database
define('DB_USER', 'username');

// You need to replace userpass with the user password of the database
define('DB_PASS', 'userpass');

// Then remove the '-example' from 'bdd-example.php' to get 'bdd.php'
